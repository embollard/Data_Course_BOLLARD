##write a loop print out penguin, bird, fish, whale
for (i in 1:5) {
  print(1)
}
vector = c(1,2,3,4)

cha = c('penguin', 'bird', 'fish', 'whale')

vector = c(1,2,3,4) logic, numeric, character

y= c('penguin', 1, TRUE) gives a result of the characters.

cha = c("penguin", "bird", "fish", "whale") #inside the quotes is an element. 
cha1 = [penguin]
cha2 = [bird]
cha3 = [fish]
cha4 = [whale]

##create a new column for what fruit each student likes in dat_BIOL3100. 
## fill in with vector fruit

new_df <- data.frame(
  name = c('Evangeline', 'Hasan', 'Tyler')
  favorite_no = c()
)

## create a new object called 'car_4' save only cars with 4 cylinders

df_cars = mtcars

car_4 = df_cars[mtcars$cyl == 4,] 

#filter by mileage and something else
